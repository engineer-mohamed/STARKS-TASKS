#include <stdio.h>
#include <stdlib.h>

int main()
{
    int ID , password;
    printf("Enter your ID : ");
    scanf("%i",&ID);
    if(ID == 123)
    {
        printf("Enter the password : ");
        scanf("%i",&password);
        if(password == 123)
        {
            printf("you are welcomed");
        }
        else
        {
            for(int i=0;i<2;i++)
            {
                printf("password is wrong!! , reenter the password : ");
                scanf("%i",&password);
                if(password == 123)
                printf("you are welcomed");


            }
            if(password!=123)
            printf("No more tries");
        }
    }
    else
        printf("You are not registered");
    return 0;
}
